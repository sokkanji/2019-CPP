/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/createPortal-vs-dialog-element",
  experimental: {
    appDir: true,
  },
};

module.exports = nextConfig;
